package co.inboxies.app

/**
 * App entry naming mirror of iOS `InboxiesApp`.
 * The Android [Application] subclass is [InboxiesApplication] (see AndroidManifest).
 */
typealias InboxiesApp = InboxiesApplication
